<% if(success && success.length) {%>
    <div class="alert alert-success aler-dismisable fade show" role="alert">
        <%= success %>
            <button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% } %>

        <% if(error && error.length) {%>
            <div class="alert alert-danger aler-dismisable fade show" role="alert">
                <%= error %>
                    <button class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            <% } %>